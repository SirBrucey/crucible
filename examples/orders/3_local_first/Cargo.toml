# Its own workspace, not a member of the examples one: this is the only example
# that loads crucible's span adapter, and a path dependency reaching out of the
# examples directory would have to resolve for every example built from it.
[workspace]
resolver = "3"
members = ["api", "inventory"]

[workspace.package]
edition = "2024"
authors = ["Stuart Bruce"]
publish = false
license = "UNLICENSED"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
missing_errors_doc = "allow"
missing_panics_doc = "allow"
must_use_candidate = "allow"

[workspace.dependencies]
anyhow = "1"
axum = "0.8"
crucible-span-rust = { path = "../../../crucible-span-rust" }
futures-util = "0.3"
lapin = "3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "tls-rustls", "mysql", "sqlite", "macros"] }
tokio = { version = "1", features = ["fs", "io-util", "macros", "net", "rt-multi-thread", "signal", "sync", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
